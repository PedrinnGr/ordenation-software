# Como executar o programa
Primeiro, compile o programa com o comando: 
```bash
make
```

Depois, execute o programa com o comando:
```bash
./ordenationSoftware <diretório com arquivos de dados>
```
